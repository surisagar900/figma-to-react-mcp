{
  "name": "Frontend Dev MCP Server",
  "description": "Unified MCP server for frontend developers with GitHub, Figma, and Playwright integrations",
  "version": "1.0.0",
  "mcpServers": {
    "frontend-dev": {
      "command": "npx",
      "args": ["frontend-dev-mcp-server"],
      "env": {
        "GITHUB_TOKEN": "your_github_token_here",
        "FIGMA_ACCESS_TOKEN": "your_figma_token_here",
        "WORKSPACE_PATH": "${workspaceFolder}"
      }
    }
  },
  "tools": [
    {
      "name": "design_to_code",
      "description": "Convert Figma design to React component",
      "usage": "Paste Figma URL with selected frame/component"
    },
    {
      "name": "test_design_implementation",
      "description": "Test component against Figma design",
      "usage": "Provide Figma URL and component URL"
    },
    {
      "name": "analyze_figma_design",
      "description": "Extract design tokens and components",
      "usage": "Paste any Figma file URL"
    }
  ],
  "setup": {
    "requirements": [
      "Node.js 18+",
      "GitHub Personal Access Token",
      "Figma Access Token"
    ],
    "steps": [
      "1. Run: npx frontend-dev-mcp-server --setup",
      "2. Follow the prompts to enter your tokens",
      "3. The configuration will be automatically added to Cursor"
    ]
  }
}
